load(":rules.bzl", "pkg_tar", "file_size")

pkg_tar(
    name = "archive",
    srcs = [
        "1.txt",
        "2.txt",
    ],
    package_dir = "/my/layout",
)

file_size(
    name = "size",
    file = ":archive",
)
